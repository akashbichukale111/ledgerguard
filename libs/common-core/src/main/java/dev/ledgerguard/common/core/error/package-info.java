/**
 * Typed errors and the stable error-code catalogue.
 *
 * <p>Framework-free by design: the HTTP mapping to RFC 9457 Problem Details lives in the adapter
 * layer, so the domain can raise meaningful errors without depending on Spring.
 */
package dev.ledgerguard.common.core.error;
