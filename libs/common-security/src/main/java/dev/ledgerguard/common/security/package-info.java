/**
 * Security building blocks.
 *
 * <p>OAuth2 resource-server configuration, the role model and RBAC annotations, and the
 * redaction machinery that keeps counterparty identifiers out of logs.
 */
package dev.ledgerguard.common.security;
