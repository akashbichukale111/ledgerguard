<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>dev.ledgerguard</groupId>
    <artifactId>ledgerguard-services</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>ledgerguard-gateway</artifactId>
  <description>Edge: routing, correlation-ID minting and security headers. No business logic.
    JWT validation and rate limiting are named in the architecture but are NOT implemented here —
    see docs/phase-reports/phase-19.md for what each needs first.</description>

  <dependencies>
    <!-- Reactive gateway. The starter was renamed in the Spring Cloud 2025.x train; the older
         spring-cloud-starter-gateway coordinate does not resolve against this BOM. -->
    <dependency>
      <groupId>org.springframework.cloud</groupId>
      <artifactId>spring-cloud-starter-gateway-server-webflux</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.projectreactor</groupId>
      <artifactId>reactor-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
