/**
 * Edge gateway.
 *
 * <p>Spring Cloud Gateway: routing, JWT validation, per-principal rate limiting,
 * correlation-ID minting, security headers and WebSocket upgrade proxying.
 *
 * <p>Deliberately contains no business logic. Its boundary exists because edge concerns
 * scale and fail differently from domain processing.
 */
package dev.ledgerguard.gateway;
