/**
 * Read side.
 *
 * <p>Kafka-fed projections in MongoDB, Transaction 360 assembly, the keyset-paginated grid
 * API, the hash-chained audit log in PostgreSQL and STOMP fan-out to the console.
 *
 * <p>Its projections are eventually consistent and the console surfaces the lag rather
 * than hiding it.
 */
package dev.ledgerguard.query;
