<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>dev.ledgerguard</groupId>
    <artifactId>ledgerguard-services</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>reconciliation-service</artifactId>
  <description>Saga orchestration, the matching engine and the exception lifecycle.</description>

  <dependencies>
    <dependency>
      <groupId>dev.ledgerguard</groupId>
      <artifactId>common-core</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>dev.ledgerguard</groupId>
      <artifactId>common-observability</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>dev.ledgerguard</groupId>
      <artifactId>contracts</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <!--
      This service serves no business endpoint, but the web starter is not optional here.
      Two things depend on it:
        1. Boot's auto-configured ObjectMapper is built by Jackson2ObjectMapperBuilder, which lives
           in spring-web. jackson-databind alone (which arrives transitively) makes the code compile
           but leaves no ObjectMapper *bean*, so anything injecting one fails the context at boot.
        2. management.endpoints.web and the compose healthcheck both address actuator over HTTP.
           Without a servlet container nothing binds 8080 and the container never turns healthy.
    -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.kafka</groupId>
      <artifactId>spring-kafka</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>postgresql</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-params</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>net.jqwik</groupId>
      <artifactId>jqwik</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- Mutation testing, scoped to the matching engine only.
           PIT measures whether tests ASSERT, not merely whether they execute lines — a far
           stronger signal than coverage. It is slow, so it is pointed at the package where a
           silent defect is most expensive and nowhere else. Run with: mvn -Ppit test -->
      <plugin>
        <groupId>org.pitest</groupId>
        <artifactId>pitest-maven</artifactId>
        <version>1.17.4</version>
        <configuration>
          <targetClasses>
            <param>dev.ledgerguard.reconciliation.domain.matching.*</param>
          </targetClasses>
          <targetTests>
            <param>dev.ledgerguard.reconciliation.matching.ReconciliationEngineTest*</param>
          </targetTests>
          <excludedTestClasses>
            <!-- The Testcontainers saga IT needs Docker, and jqwik uses its own JUnit engine.
                 Neither belongs in a mutation run scoped to the pure matching engine. -->
            <param>dev.ledgerguard.reconciliation.saga.*</param>
            <param>dev.ledgerguard.reconciliation.matching.ReconciliationEnginePropertyTest</param>
          </excludedTestClasses>
          <outputFormats>
            <param>XML</param>
            <param>HTML</param>
          </outputFormats>
          <timestampedReports>false</timestampedReports>
        </configuration>
        <dependencies>
          <dependency>
            <groupId>org.pitest</groupId>
            <artifactId>pitest-junit5-plugin</artifactId>
            <version>1.2.2</version>
          </dependency>
        </dependencies>
      </plugin>

      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <!-- See transaction-service: without a classifier, repackage replaces the jar and
               failsafe loses the application classes. -->
          <classifier>boot</classifier>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>repackage</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
